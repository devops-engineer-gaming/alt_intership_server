# alt_intership_server

## Usage

1.Install requirements  
`pip install -r requirements.txt`  
2. Startup your database (check DATABASE_URL in settings.py)  
3. Run server
`uvicorn --reload server:app` 
